#include<stdio.h>
#include<math.h>

int main()
{
    int i,n,t,j;
    scanf("%d",&n);
    for(i=1 ; i<=n ; i++)
    {
        scanf("%d",&t);
        for(j=1 ; j<=n ; j++)
        {

        }
         if(sqrt(t)*sqrt(t)==t)
            {
                printf("yes\n");
            }
            else
                printf("no\n");
    }

    return 0;
}
